living_room_brighten:
  alias: Brighten living room lights
  sequence:
  - service: light.turn_on
    data:
      entity_id: light.living_room
      brightness_template: '{{ (state_attr('light.living_room', 'brightness') + 25) | int }}'
