living_room_lights_on:
  alias: Turn Living Room Lights On
  sequence:
    - service: light.turn_on
      data_template:
        entity_id: light.living_room
        brightness: >
          {%- if true %}
            {{ states('counter.global_fade_counter') * 5 | round(0) | int }}
          {%- endif %}
