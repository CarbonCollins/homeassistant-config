landing_lights_on:
  alias: Turn Landing Lights On
  sequence:
    - service: light.turn_on
      data_template:
        entity_id: light.landing
        brightness: >
          {%- if is_state('input_boolean.late_night', 'on') %}
            128
          {%- elif now().hour| int >= 22 %}
            50
          {%- elif now().hour| int < 6 %}
            5
          {%- elif now().hour| int < 8 %}
            50
          {%- else %}
            200
          {%- endif %}
