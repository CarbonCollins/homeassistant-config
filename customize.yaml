sensor.kitchen_motion_sensor:
  device_class: motion
sensor.living_room_motion_sensor:
  device_class: motion
binary_sensor.apollo:
  friendly_name: Apollo
  templates:
    icon: >
      if (state === 'on') return 'mdi:server-security';
      return 'mdi:server-off';